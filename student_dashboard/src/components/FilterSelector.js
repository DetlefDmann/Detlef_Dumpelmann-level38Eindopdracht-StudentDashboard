import React from 'react'

const FilterSelector = () => {
    return (
        <section>
            place inputs for selecting student/ assignments here
        </section>
    )
}

export default FilterSelector
